{% extends 'base.html.twig' %}

{% block body %}
    <div {{ react_component('DarkMode/Index') }}>
        Loading... <i class="fas fa-cog fa-spin fa-3x"></i>
    </div>
    
    <div {{ react_component('ProductList') }}>
        Loading... <i class="fas fa-cog fa-spin fa-3x"></i>
    </div>
{% endblock %}